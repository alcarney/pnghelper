# v0.1

+ The project now builds into an actual shared object now (aka library)

# v0.0.5

+ Ability to load images from a file
+ Ability to save images to a file
+ Ability to pass a function to an iterator which woulditerate over all the pixels 
  in the image and apply that function to them
